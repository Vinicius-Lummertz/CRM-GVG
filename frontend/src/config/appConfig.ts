// Global app configuration
// Set USE_MOCKS to true to force the frontend to use mock data
// Set to false to use real backend APIs (controlled by services in /backend)
export const USE_MOCKS = true;

// Optional: export other global flags here in future
